[install]
is_configured = 0
build         = 1

[ui]
is_visible    = 1
label         = SQL Server Fleet Monitoring

[launcher]
author        = Greenmtnsun
description   = Event-driven SQL Server fleet monitoring — dashboards (Executive / Operations / Security in both SimpleXML and Studio), alerts (P1/P2/P3), and scheduled reports. Uses only Windows Event Log data; no add-ons required.
version       = 1.0.0

[package]
id            = sql_fleet
check_for_updates = false
